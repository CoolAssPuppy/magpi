node_modules
.next
coverage
playwright-report
test-results
pnpm-lock.yaml
design

# Python virtualenvs and caches
.venv
__pycache__

# Supabase CLI scratch state
api/supabase/.branches
api/supabase/.temp
supabase/.temp

# Written by `vercel link`
.vercel

# Planning notes and the pasted brief. Prettier reflows the tables and then
# fails its own --check on the result.
tasks/
docs/BRIEF.md

# Generated by `pnpm previews:fixtures` from the real Python pages. Prettier
# reflows it, and then previews:fixtures:check reports the file as stale, so
# the two gate steps can never both pass. The generator owns the formatting.
web/tests/fixtures/preview-fixtures.json

# TypeScript incremental build cache
*.tsbuildinfo
