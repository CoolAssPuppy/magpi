# Tech debt audit

Generated 2026-09-09, against `108ea20`, by four readers: one per module plus a
synthesis pass. Every finding carries a `file:line`. Findings marked **verified**
were reproduced independently rather than taken from a report.

## Executive summary

1. **A member can move a space into another organization, and can escalate a
   team space to `org` kind.** Both reproduced against the live database.
   `spaces_update_member` tests only membership and `95_grants.sql` grants the
   whole table, so `org_id` and `kind` are writable by anyone in the space. The
   content follows and is then metered and billed against an org it does not
   belong to. This is the worst finding.
2. **A caller can point a document at another space's storage object.**
   `storagePath` is validated as `z.string().min(1)` and handed to the service
   client verbatim; the ingest worker reads whatever path the row names with no
   space check. Cross-space read.
3. **Three production cron paths resolved to nothing.** Fixed in `108ea20`.
   Nothing ingested, synced or dreamed on a deploy, and no gate step looked at
   `vercel.json`.
4. **Two public Edge Functions would have answered 401.** Fixed in `108ea20`.
   No `[functions.*]` block, so `verify_jwt` defaulted true on the OAuth
   callback and the Stripe webhook.
5. **The retry budget is unreachable.** A transient provider error writes
   terminal `failed`, so `attempts < 3` in `claim_ingest_jobs` can never fire.
   The SQL comment describes retries that cannot happen.
6. **Two recovery mechanisms fight and the worse one wins.** A JS sweep and the
   SQL reclaim CTE cover the same rows with opposite outcomes; the sweep runs
   first, so a crashed job is retired instead of retried and the CTE is dead.
7. **Notion and Linear first passes can silently drop a backlog.** Both advance
   the cursor to the newest item seen rather than the page cursor, which is only
   correct if the provider sorts ascending. Notion's own query sorts descending.
8. **Eleven of eighteen functions are revoked only in a migration, not in
   `schemas/`.** The exact drift `docs/lessons.md` was written about, written by
   the person who wrote the entry.
9. **Two meters are read and never written.** `usage_events` with `kind =
'query'` and `documents.last_retrieved_at`, so the query plan limit is
   enforced by nothing and the dead-content panel reports everything as dead.
10. **Two docs assert things that do not exist.** `docs/retrieval.md` says four
    chunking rules are implemented and none is; `docs/mobile-spec.md` cites a
    string catalog module that was never written.

## Architectural mental model

Three tiers with a deliberate seam between them.

`web/` is a Next App Router app. Server components read through a per-request
Supabase client carrying the caller's JWT, so RLS is the authorization; server
actions all funnel through `withSession`; one route handler streams chat because
a server action cannot. The service-role client exists in `lib/supabase/service`
and is meant to appear only after the caller and their rights are already
established.

`supabase/functions/` is Deno. The design rule that matters: a job body is a
plain async function taking a record and injected clients, and the runtime entry
point is a thin wrapper, so moving off Edge Functions is a wrapper change. That
rule holds in the code. Four source drivers sit behind one contract and a
registry, and no provider slug appears in a branch outside it, which I checked
specifically.

`supabase/schemas/` is the source of truth; migrations are generated. The
permission model is spaces, and `space_id` is denormalized onto `chunks` so the
RLS filter is one indexed column. Composite foreign keys carry `space_id`
through every reference so a cross-space write is refused by the database rather
than by convention.

**Where the model and the code disagree**: the same `space_id` discipline was
never applied to `org_id`, which is finding F001. And the seam between the
generated schema and the hand-written migrations leaks in both directions,
which is F008.

## Findings

| ID   | Category       | File:Line                                                                                                     | Sev      | Eff | Description                                                                                                                                                                                                                                                                                                                                                                                           | Recommendation                                                                                                                                                                                                                                                                           |
| ---- | -------------- | ------------------------------------------------------------------------------------------------------------- | -------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F001 | Security       | `supabase/schemas/90_policies.sql:52`, `95_grants.sql:25`                                                     | Critical | M   | **Fixed.** A space member could `update spaces set org_id = <other org>` and flip a team space to `kind = 'org'`, and nothing constrained `documents.org_id`, `chunks.org_id`, `connections.org_id`, `ingest_jobs.org_id` to match.                                                                                                                                                                   | Done. Grant narrowed to `(name, dreaming_enabled)`, `spaces (id, org_id)` unique added, and the four content tables now carry `org_id` in a composite key. Three pgTAP assertions.                                                                                                       |
| F002 | Security       | `web/app/(app)/documents/actions.ts:11,49`                                                                    | Critical | S   | **Fixed.** `storagePath` was `z.string().min(1)`, written by the service client, and `jobs/ingest.ts:111` read that path with no space check, so a caller could index another space's object into their own.                                                                                                                                                                                          | Done. The action takes an `objectName` with no slash in it and builds the path from the space it already checked. `mimeType` is now `z.enum`. See also F071.                                                                                                                             |
| F003 | Correctness    | `supabase/functions/_shared/jobs/ingest.ts:296`                                                               | Critical | S   | **Fixed.** A transient `SourceError` wrote terminal `failed`, so the `attempts < 3` budget was unreachable and the driver message promising a retry was false.                                                                                                                                                                                                                                        | Done. `needsReconnect === false` writes `queued` and the attempt cap in `claim_ingest_jobs` retires it.                                                                                                                                                                                  |
| F004 | Correctness    | `supabase/functions/ingest-worker/index.ts:37`, `schemas/80_functions.sql:318`                                | Critical | M   | **Fixed.** The JS `retireAbandoned` sweep and the SQL reclaim CTE covered the same rows with opposite outcomes, and the sweep ran first, so the CTE never matched.                                                                                                                                                                                                                                    | Done. The sweep is gone from `ingest-worker`; `dream-worker` still uses it and has no reclaim of its own.                                                                                                                                                                                |
| F005 | Correctness    | `supabase/functions/_shared/sources/notion.ts:190`                                                            | Critical | M   | **Fixed.** The first pass over a workspace larger than `MAX_REQUESTS` pages advanced the cursor to the newest page while search sorts descending, so the backlog was never read.                                                                                                                                                                                                                      | Done. `sources/cursor.ts` makes a cursor either a watermark or a backlog position, and both drivers have a two-pass test.                                                                                                                                                                |
| F006 | Correctness    | `supabase/functions/_shared/sources/linear.ts:211`                                                            | High     | M   | **Fixed.** The cursor advanced to the newest `updatedAt` from one page of 50, and `orderBy: updatedAt` carried no direction, so only our own fixture said ascending.                                                                                                                                                                                                                                  | Done. Paginates with the provider page token, same discriminated cursor as Notion, with a two-pass test.                                                                                                                                                                                 |
| F007 | Correctness    | `supabase/functions/connections-claim/index.ts:90`                                                            | High     | S   | **Fixed.** `.eq('external_account_id', ...)` never matches a null, so a provider returning no account label created a fresh connection with a live token on every reconnect.                                                                                                                                                                                                                          | Done. `.is()` when there is no account, and two partial unique indexes so the database refuses it too. A single index over the nullable column would not have: unique treats every null as distinct, which is the half that broke.                                                       |
| F008 | Schema drift   | `supabase/schemas/80_functions.sql:13` and 10 others                                                          | High     | S   | **Fixed.** Eleven of eighteen functions were revoked only in `migrations/20260909161000`, so a shadow database built from `schemas/` alone left them executable by PUBLIC.                                                                                                                                                                                                                            | Done. Revokes are in `80_functions.sql` beside each grant, with a pgTAP set comparison over `proacl` verified by breaking it.                                                                                                                                                            |
| F009 | Correctness    | `web/lib/analytics/queries.ts:281`                                                                            | High     | S   | **Fixed.** `usage_events` with `kind = 'query'` was read and written nowhere, so the meter read zero and `plan_monthly_query_limit` was enforced by nothing.                                                                                                                                                                                                                                          | Done. The answer path writes one row per answered question and `check_query_allowed` gates the chat route with a 402.                                                                                                                                                                    |
| F010 | Correctness    | `web/lib/analytics/queries.ts:194`                                                                            | High     | M   | **Fixed.** `documents.last_retrieved_at` and `retrieval_count` were written by nothing, so the dead-content panel reported every document as dead.                                                                                                                                                                                                                                                    | Done. `record_retrieval` is called with the distinct document ids a search returned, scoped to the caller's own spaces.                                                                                                                                                                  |
| F011 | Error handling | `web/app/(app)/chat/actions.ts:40,57,74`, `spaces/actions.ts:30,38,56,75`, `documents/actions.ts:44,62,71,90` | High     | M   | **Fixed.** Eleven paths returned the raw `PostgrestError` string, so a policy denial rendered as `new row violates row-level security policy for table "conversations"`.                                                                                                                                                                                                                              | Done. `web/lib/actions/database-error.ts` logs the raw error and returns product copy, with an optional per-SQL-state map.                                                                                                                                                               |
| F012 | Correctness    | `web/lib/chat/answer.ts:103`                                                                                  | High     | S   | **Fixed.** `setCondensedQuery` and `setTitle` ran inside the try that yields the error event, after `done`, so a failed title write deleted a delivered answer from the screen.                                                                                                                                                                                                                       | Done. Own try around the post-`done` block, which logs and yields nothing.                                                                                                                                                                                                               |
| F013 | Testing        | `supabase/functions/_shared/jobs/dream_test.ts:230`                                                           | High     | S   | **Fixed.** The stub returned the foreign chunk regardless of `space_id=eq.` and the assertion greped for a uuid that could never appear, so a security test proved nothing.                                                                                                                                                                                                                           | Done. The stub applies the filters the read carried. Verified by deleting the space filter, which fails both tests.                                                                                                                                                                      |
| F014 | Testing        | `supabase/functions/_shared/sources/leak_test.ts:115`                                                         | High     | S   | **Fixed.** Eight leak tests exercised `refresh` on drivers whose refresh returns `not_supported` without touching `deps`, so they passed against an empty function body. There were ten, not eight.                                                                                                                                                                                                   | Done. Drivers are discovered rather than listed, and the two that trade nothing assert a request count of zero.                                                                                                                                                                          |
| F015 | Dead code      | `supabase/functions/_shared/oauth.ts:316`                                                                     | High     | S   | **Fixed.** `OAuthDriver.refreshTokens` was dead, and it was the copy that knew the `basicAuthForToken` and `normalizePayload` quirks the live path did not.                                                                                                                                                                                                                                           | Done. Deleted, and the quirks exported so the live refresh reads the same table. Found a latent Slack bug on the way.                                                                                                                                                                    |
| F016 | Architecture   | `supabase/functions/_shared/sources/contract.ts:41`                                                           | High     | M   | **Fixed.** `ChangePage.hasMore` was computed by four drivers, asserted in eight tests, and read by no caller, so a partial sync never scheduled another pass.                                                                                                                                                                                                                                         | Done. `runSyncJob` walks until the driver is done, the budget is spent, or the cursor stops moving.                                                                                                                                                                                      |
| F017 | Performance    | `web/app/(app)/admin/members/page.tsx:58`                                                                     | High     | M   | **Fixed.** One `auth.admin.getUserById()` per member inside `Promise.all`, over an unlimited query, at a stated scale of 4,000 people.                                                                                                                                                                                                                                                                | Done. Fifty members a page, and `org_member_emails` reads their addresses in one query with its own admin test.                                                                                                                                                                          |
| F018 | Docs           | `docs/retrieval.md:100`                                                                                       | High     | S   | **Fixed.** Said "Status: implemented" over a 100-token merge, a 1,200-token ceiling, a heading rule and real tokenization, none of which exists.                                                                                                                                                                                                                                                      | Done. Rewritten from the code, with file:line for every claim.                                                                                                                                                                                                                           |
| F019 | Docs           | `docs/mobile-spec.md:46`                                                                                      | High     | S   | **Fixed.** Named `web/lib/strings/` as the string catalog and cited keys from it. The module has never existed.                                                                                                                                                                                                                                                                                       | Done. Rewritten to cite what exists.                                                                                                                                                                                                                                                     |
| F020 | Correctness    | `supabase/functions/dream-run/index.ts:55`                                                                    | High     | S   | **Fixed.** Inserted a run as `queued` then ran it inline without claiming, so a cron tick could execute the same run twice.                                                                                                                                                                                                                                                                           | Done. The insert writes `running` and its own `started_at`, so there is no window to lose rather than a claim to lose.                                                                                                                                                                   |
| F021 | Security       | `web/lib/chat/prompt.ts:25`                                                                                   | Medium   | M   | **Fixed.** Retrieved content, untrusted and arriving from Slack and Notion, was injected at system role with no delimiting.                                                                                                                                                                                                                                                                           | Done. User role, inside delimiters a passage cannot write itself, and the instruction says to treat it as content.                                                                                                                                                                       |
| F022 | Performance    | `web/lib/supabase/context.ts:22`                                                                              | Medium   | S   | **Fixed.** `getSessionContext` was not wrapped in `cache()`, so an admin page load made three `auth.getUser()` round trips plus three `org_members` selects.                                                                                                                                                                                                                                          | Done. Wrapped in `cache()`.                                                                                                                                                                                                                                                              |
| F023 | Correctness    | `web/components/documents/upload-panel.tsx:42`                                                                | Medium   | M   | **Fixed.** The dedupe ref keyed on file name only and the effect read live `spaceId`, so the same filename uploaded to a second space was silently never indexed.                                                                                                                                                                                                                                     | Done. Keyed on the full object path, and the space is captured when the upload starts.                                                                                                                                                                                                   |
| F024 | Performance    | `web/components/connections/sync-activity.tsx:51`                                                             | Medium   | S   | **Fixed.** The effect depended on an array rebuilt every render, so every status change tore down and resubscribed the Realtime channel.                                                                                                                                                                                                                                                              | Done. Three renders opened three channels; the test proves one.                                                                                                                                                                                                                          |
| F025 | Testing        | `web/vitest.config.mts:11,16`                                                                                 | Medium   | M   | **Fixed.** `hooks/**` was in neither the test include nor the coverage include and had no tests, roughly 450 lines including the two hooks `decisions.md` records as deliberately rewritten. Measured: excluding them the suite read 96.16/92.56/97.52/96.82, including them and untested it read 90.51/88.61/90.79/90.95, below all four thresholds. The 97/93/99/98 in this row was never measured. | Done. Both globs, `coverage.reportOnFailure`, and tests for all four hooks at 100%. Now 96.43/92.97/97.72/97.06 with thresholds unchanged at 95/90/95/95.                                                                                                                                |
| F026 | Error handling | `web/app/(app)/connections/error.tsx:17` and 6 others                                                         | Medium   | S   | **Fixed.** Seven of thirteen boundaries rendered `error.message`, which Next replaces with digest text in production. Two had no reset. None logged the digest.                                                                                                                                                                                                                                       | Done. All thirteen share one component that logs `error.digest`.                                                                                                                                                                                                                         |
| F027 | Types          | `web/lib/spaces/spaces.ts:66`, `documents.ts:89`                                                              | Medium   | S   | **Fixed.** Two assertions bridged a query into a hand-written row type that shadowed the generated one.                                                                                                                                                                                                                                                                                               | Done. Both hand-written types and both assertions are gone.                                                                                                                                                                                                                              |
| F028 | Types          | `web/app/auth/confirm/route.ts:10`                                                                            | Medium   | S   | **Fixed.** `searchParams.get('type') as EmailOtpType` asserted an untrusted query parameter and handed it to `verifyOtp`.                                                                                                                                                                                                                                                                             | Done. Parsed with `z.enum` over the real set.                                                                                                                                                                                                                                            |
| F029 | Correctness    | `web/lib/documents/documents.ts:79`                                                                           | Medium   | S   | **Fixed.** The embedded `ingest_jobs` had no order or limit, so `row.ingest_jobs[0]` was arbitrary and the list could disagree with the detail page.                                                                                                                                                                                                                                                  | Done. Ordered and limited to match the detail page.                                                                                                                                                                                                                                      |
| F030 | Performance    | `web/lib/chat/store.ts:113`                                                                                   | Medium   | S   | **Fixed.** `loadMessages` selected every message in a conversation on every POST and used eight turns of it.                                                                                                                                                                                                                                                                                          | Done. Both it and the chunks query take the limit the prompt builder uses.                                                                                                                                                                                                               |
| F031 | Design         | `web/styles/tokens.css:19`                                                                                    | Medium   | S   | **Fixed.** The named z-index scale had zero consumers; five overlay primitives used a raw `z-50` colliding with `--z-toast`.                                                                                                                                                                                                                                                                          | Done. The scale is applied to the overlay primitives.                                                                                                                                                                                                                                    |
| F032 | Design         | `web/components/ui/dialog.tsx:41` and 5 others                                                                | Medium   | M   | **Fixed.** Six elements paired `border` with `shadow-lg`, which `DESIGN.md` bans by name, and used `rounded-md` where `--radius-panel` exists.                                                                                                                                                                                                                                                        | Done.                                                                                                                                                                                                                                                                                    |
| F033 | Design         | `web/components/ui/*` (30+ sites)                                                                             | Medium   | M   | **Fixed.** The primitives were written against shadcn compat aliases that resolve only through `compat.css`, which upstream has marked for deletion.                                                                                                                                                                                                                                                  | Done. Rewritten onto the real tokens, so the next token sync cannot unstyle them.                                                                                                                                                                                                        |
| F034 | Consistency    | `web/app/(app)/documents/page.tsx:12`, `spaces/page.tsx:11`                                                   | Medium   | S   | **Fixed.** Two ways to get a server client in a page: four resolved `getSessionContext()`, two called `createClient()` directly and had no `orgId`.                                                                                                                                                                                                                                                   | Done. All six resolve `getSessionContext()`.                                                                                                                                                                                                                                             |
| F035 | Observability  | `web/lib/openai/call.ts:116`                                                                                  | Medium   | M   | **Fixed.** A cancelled stream unwound the generator at a yield, skipping the catch and the trailing report, so an abandoned answer was billed by OpenAI and metered by nothing.                                                                                                                                                                                                                       | Done. `try/finally`, reported from the finally.                                                                                                                                                                                                                                          |
| F036 | Error handling | `web/app/api/chat/route.ts:105`                                                                               | Medium   | S   | **Fixed.** `consumeRateLimit` threw and nothing caught it, so the request 500d, contradicting the comment three lines above promising a typed JSON body.                                                                                                                                                                                                                                              | Done. Caught, logged, and answered with a typed `server_error`.                                                                                                                                                                                                                          |
| F037 | Security       | `web/next.config.ts:9`                                                                                        | Medium   | S   | **Fixed.** No `headers()` block at all.                                                                                                                                                                                                                                                                                                                                                               | Done. CSP pinned to the one Supabase origin, with the nonce trade recorded in the file.                                                                                                                                                                                                  |
| F038 | Security       | `web/app/auth/error/page.tsx:25`                                                                              | Medium   | S   | **Fixed.** The auth error page interpolated a raw provider message.                                                                                                                                                                                                                                                                                                                                   | Done.                                                                                                                                                                                                                                                                                    |
| F039 | Correctness    | `supabase/functions/_shared/billing.ts:309`                                                                   | Medium   | S   | **Fixed.** `applyCheckout` set `plan: 'team'` unconditionally, so any product or an unpaid session got Team.                                                                                                                                                                                                                                                                                          | Done. Reads `payment_status` and `metadata.plan`, and leaves a subscription's plan to the event that knows the price.                                                                                                                                                                    |
| F040 | Correctness    | `supabase/functions/_shared/jobs/ingest.ts:218`                                                               | Medium   | S   | **Fixed.** A job killed downloading reported the stage it was about to enter, so every timeout in fetch was filed as extract.                                                                                                                                                                                                                                                                         | Done for every stage, not only fetch. The audit's recommended checkpoint could not have fixed it.                                                                                                                                                                                        |
| F041 | Testing        | `supabase/functions/_shared/jobs/sync_test.ts:314`                                                            | Medium   | S   | **Fixed.** The timeout test died at the credentials checkpoint, before `listChanges` was called, so it could not observe the timeout it existed for.                                                                                                                                                                                                                                                  | Done. A jumping clock across twenty ticks. Verified by moving `advanceCursor`, which the old test could not catch.                                                                                                                                                                       |
| F042 | Error handling | `supabase/functions/connections-claim/index.ts:172`                                                           | Medium   | S   | **Fixed.** `populatePicker` caught everything, so a decrypt failure returned a successful claim with an empty picker and no signal.                                                                                                                                                                                                                                                                   | Done. Narrowed to `SourceError`; anything else writes `status_detail`.                                                                                                                                                                                                                   |
| F043 | Performance    | `supabase/functions/token-refresh/index.ts:36`                                                                | Medium   | S   | **Fixed.** Tokens were decrypted for connections that did not need renewing.                                                                                                                                                                                                                                                                                                                          | Done. `refreshIfSpent` returns the ciphertext it did not open.                                                                                                                                                                                                                           |
| F044 | Types          | `supabase/functions/connections-claim/index.ts:58`, `connections-callback/index.ts:79`                        | Medium   | S   | **Fixed.** The two RPC results carrying a PKCE verifier and an encrypted token were cast, while the one carrying least was parsed.                                                                                                                                                                                                                                                                    | Done. Both parse, and null and malformed are different answers.                                                                                                                                                                                                                          |
| F045 | Error handling | `supabase/functions/ingest-worker/index.ts:51`                                                                | Medium   | S   | **Fixed.** The claim rpc and its parse sat inline in the worker.                                                                                                                                                                                                                                                                                                                                      | Done. Behind one function that answers with a typed error.                                                                                                                                                                                                                               |
| F046 | Performance    | `supabase/functions/_shared/jobs/dream_entities.ts:50`                                                        | Medium   | M   | **Fixed.** A hundred entities took two hundred statements.                                                                                                                                                                                                                                                                                                                                            | Done. One upsert and one mention insert, with ids matched by conflict key.                                                                                                                                                                                                               |
| F047 | Performance    | `supabase/functions/_shared/jobs/dream_connections.ts:74`                                                     | Medium   | M   | **Fixed.** One read per document in the similarity pass.                                                                                                                                                                                                                                                                                                                                              | Done. Batched.                                                                                                                                                                                                                                                                           |
| F048 | Performance    | `supabase/functions/_shared/jobs/sync.ts:93`                                                                  | Medium   | S   | **Fixed.**                                                                                                                                                                                                                                                                                                                                                                                            | Done.                                                                                                                                                                                                                                                                                    |
| F049 | Security       | `supabase/functions/_shared/http.ts:20`                                                                       | Medium   | S   | **Fixed.** `clientIp` read the rightmost `X-Forwarded-For`, which on the deployed runtime is the platform proxy, making every per-IP bucket one global bucket.                                                                                                                                                                                                                                        | Done. `cf-connecting-ip` first, which the edge writes over anything the caller sent.                                                                                                                                                                                                     |
| F050 | Correctness    | `supabase/functions/_shared/sources/common.ts:104`                                                            | Medium   | S   | **Fixed.** Two implementations of the refresh grant, and the live one knew neither quirk.                                                                                                                                                                                                                                                                                                             | Done. One implementation, reading the quirks table in `oauth.ts`.                                                                                                                                                                                                                        |
| F051 | Consistency    | `web/app/(app)/chat/actions.ts:26`, `settings/actions.ts:18`                                                  | Medium   | S   | **Fixed.** Zod parsing happened outside `withSession` in two action files and inside in two others.                                                                                                                                                                                                                                                                                                   | Done. Inside, in all four.                                                                                                                                                                                                                                                               |
| F052 | Correctness    | `web/app/(app)/documents/actions.ts:73`                                                                       | Medium   | S   | **Fixed.** The `usage_events` insert result was discarded, and it is the row the document meter is computed from.                                                                                                                                                                                                                                                                                     | Done. Checked, with copy saying the file was saved and queued but not counted.                                                                                                                                                                                                           |
| F053 | Consistency    | `web/app/(app)/admin/billing/page.tsx:60`                                                                     | Medium   | S   | **Fixed.** The billing page assumed Stripe was configured.                                                                                                                                                                                                                                                                                                                                            | Done. `isBillingConfigured()` in `lib/billing/config.ts`.                                                                                                                                                                                                                                |
| F054 | Dead code      | `web/components/ui/{badge,card,separator,sonner,tabs,tooltip}.tsx`                                            | Low      | S   | **Fixed.** Six primitives with zero importers, and four dependencies only they used.                                                                                                                                                                                                                                                                                                                  | Done. Deleted, with the lockfile regenerated.                                                                                                                                                                                                                                            |
| F055 | Dead code      | `web/hooks/use-infinite-query.ts` and 60 sites                                                                | Low      | S   | **Fixed.** 17 exports with no referent outside their own file.                                                                                                                                                                                                                                                                                                                                        | Done. `components/ui/**` left alone, per `decisions.md`.                                                                                                                                                                                                                                 |
| F056 | Dead code      | `supabase/functions/_shared/jobs/budget.ts:46`                                                                | Low      | S   | **Fixed.**                                                                                                                                                                                                                                                                                                                                                                                            | Done.                                                                                                                                                                                                                                                                                    |
| F057 | Dead code      | `supabase/functions/_shared/sources/contract.ts:52`                                                           | Low      | S   | **Fixed.** `ScopeOption.kind` was written by all four drivers and dropped before it reached the column; `scopeOptionSchema.url` was written by nothing.                                                                                                                                                                                                                                               | Done. The live top-level `kind` is a different field and stays.                                                                                                                                                                                                                          |
| F058 | Consistency    | `supabase/functions/_shared/jobs/claim.ts:128`                                                                | Low      | S   | **Fixed.**                                                                                                                                                                                                                                                                                                                                                                                            | Done. The claim clears `status_detail` itself.                                                                                                                                                                                                                                           |
| F059 | Docs           | `supabase/functions/_shared/model_client.ts:64`                                                               | Low      | S   | **Fixed.** A comment called an awaited insert fire and forget.                                                                                                                                                                                                                                                                                                                                        | Done. The await is right in an edge isolate; the comment now says why.                                                                                                                                                                                                                   |
| F060 | Consistency    | `supabase/functions/_shared/jobs/ingest.ts:100`                                                               | Low      | S   | **Fixed.**                                                                                                                                                                                                                                                                                                                                                                                            | Done.                                                                                                                                                                                                                                                                                    |
| F061 | Config         | `.env.example`                                                                                                | Low      | S   | **Fixed.** Six variables production code reads were undocumented.                                                                                                                                                                                                                                                                                                                                     | Done in `c1966f1`.                                                                                                                                                                                                                                                                       |
| F062 | Architecture   | `web/lib/connections/edge.ts:12`                                                                              | Low      | S   | **Fixed.**                                                                                                                                                                                                                                                                                                                                                                                            | Done. Moved to `lib/edge/invoke.ts`.                                                                                                                                                                                                                                                     |
| F063 | Duplication    | `web/lib/dreams/queries.ts:18`, `connections/queries.ts:30`, `spaces/spaces.ts:57,79`                         | Low      | S   | **Partly fixed.** Four reads of `spaces` across three files. Two were the same query on the same three columns; the other two read different column sets.                                                                                                                                                                                                                                             | Done where it was duplication: connections now calls `listSpaceOptions`. The other two stay. A shared `readSpaces(client, columns)` would take the column list as a string, and PostgREST's types are inferred from that literal, so merging them trades type inference for three lines. |
| F064 | Duplication    | `web/components/connections/connect-panel.tsx:154`                                                            | Low      | S   | **Fixed.** A raw `<select>` beside a shadcn Select elsewhere, with a shadowing local type.                                                                                                                                                                                                                                                                                                            | Done. `Pick<SpaceOption, 'id'                                                                                                                                                                                                                                                            | 'name'>` from the real type. |
| F065 | Consistency    | `web/lib/analytics/access.ts:34`, `web/app/api/stripe/session.ts:50`                                          | Low      | S   | **Fixed.** Two gates on whether billing is configured.                                                                                                                                                                                                                                                                                                                                                | Done. One.                                                                                                                                                                                                                                                                               |
| F066 | Architecture   | `web/lib/openai/call.ts:58`                                                                                   | Low      | S   | **Fixed.** `usageKindFor` sat in the module every route imports.                                                                                                                                                                                                                                                                                                                                      | Done. Its own module.                                                                                                                                                                                                                                                                    |
| F067 | Docs           | `docs/decisions.md`                                                                                           | Low      | S   | **Fixed.** The Stripe webhook was built as an Edge Function rather than the route the spec lists, which is defensible and was unrecorded.                                                                                                                                                                                                                                                             | Done. Recorded in `decisions.md`, including the `verify_jwt = false` cost.                                                                                                                                                                                                               |
| F068 | Docs           | `docs/limits.md:161`                                                                                          | Low      | S   | **Fixed.**                                                                                                                                                                                                                                                                                                                                                                                            | Done. Reconciled.                                                                                                                                                                                                                                                                        |
| F069 | Consistency    | `supabase/functions/_shared/sources/linear.ts:251`                                                            | Low      | S   | **Fixed.** The display name was passed where a slug belonged, so a refusal read `notion refused to renew`. Both callers had it, not just Linear.                                                                                                                                                                                                                                                      | Done. Slug to the log, display name to `status_detail`.                                                                                                                                                                                                                                  |
| F070 | Consistency    | `web/lib/dreams/status.ts:2`                                                                                  | Low      | S   | **Fixed.** `StatusTone` was imported from `lib/connections`, so dreams depended on connections for a type neither owns.                                                                                                                                                                                                                                                                               | Done. `lib/ui/status-tone.ts`. Moving it next to `StatusPill` as recommended would have made the first `lib` to `components` import in the repo.                                                                                                                                         |
| F071 | Correctness    | `web/components/documents/upload-panel.tsx:19`                                                                | High     | S   | **New, found fixing F002. Fixed.** The dropzone offered `.docx` and `extract.ts` has no extractor for it, so a Word document uploaded, reported success, and failed three jobs later with a message about a mime type.                                                                                                                                                                                | Done. One list in `web/lib/documents/uploads.ts`, and `scripts/check-upload-types.mjs` fails the build when it and the extractor's table disagree.                                                                                                                                       |
| F072 | Design         | `web/app/(app)/dreams/[id]/page.tsx:30` and 61 other files                                                    | High     | M   | **New, found fixing F033. Fixed.** F033 covered `web/components/ui` only. 172 more references to compat aliases lived across 61 files, so the app still unstyled when upstream deleted `compat.css`.                                                                                                                                                                                                  | Done. Eight aliases substituted for the semantic tokens they alias, `--border-stronger` defined in our own tokens, and `scripts/check-compat-tokens.mjs` in the gate.                                                                                                                    |
| F073 | Testing        | `scripts/db-test.mjs:36`                                                                                      | High     | S   | **New, from audit-infra. Fixed.** Failure detection matched `not ok`, `# Looks like you failed` and `FAILED`; pg_prove says `Result: FAIL`, `Dubious` and `Parse errors:`. A file that aborted before its first assertion passed, and nothing checked the assertion total.                                                                                                                            | Done. It requires `Result: PASS`, the file count on disk, and a non-zero assertion count. Verified by breaking a test file.                                                                                                                                                              |
| F074 | Infra          | `scripts/workflow-contract-check.mjs:18`                                                                      | Medium   | S   | **New, from audit-infra. Fixed.** `HEAVY_COMMANDS` listed pnpm script names, but every workflow invokes them by path, so `node scripts/gate.mjs` on push passed the check.                                                                                                                                                                                                                            | Done. Paths are in the list and the light gate's own invocation is removed before matching.                                                                                                                                                                                              |
| F075 | Infra          | `scripts/workflow-contract-check.mjs:43`                                                                      | Medium   | S   | **New, from audit-infra. Fixed.** `automaticTriggers` required exactly two-space indent, so `on: [push, pull_request]` returned none and the file was skipped entirely.                                                                                                                                                                                                                               | Done. Any indent, plus flow style. Verified against both forms.                                                                                                                                                                                                                          |
| F076 | Infra          | `scripts/mobile-spec-check.mjs:117`, `check-raw-color.mjs:116`                                                | Medium   | S   | **New, from audit-infra. Fixed.** Both wrapped the directory walk in a bare catch, so renaming a scan root turned the gate step into a pass. `check-compat-tokens.mjs` had the same shape when written.                                                                                                                                                                                               | Done in all three. A missing root now raises.                                                                                                                                                                                                                                            |
| F077 | Testing        | `tests/integration/claim.test.ts:104`                                                                         | High     | M   | **New, from audit-infra. Fixed.** The claim asked for the whole queue, taking the 70 jobs `seed-corpus.mjs` queues and incrementing their attempts. Three runs retired the entire demo corpus as `gave up after 3 attempts`. Reproduced.                                                                                                                                                              | Done. Foreign rows are captured before and restored after. Verified across three runs with 70 seeded jobs.                                                                                                                                                                               |
| F078 | Testing        | `tests/integration/claim.test.ts:123`                                                                         | Medium   | S   | **New, from audit-infra. Fixed.** The second `it` asserted on rows the first created, so in isolation it saw zero and failed.                                                                                                                                                                                                                                                                         | Done. Merged into the test that creates them.                                                                                                                                                                                                                                            |
| F079 | Testing        | `tests/integration/rls.test.ts:113,131,141`                                                                   | Medium   | S   | **New, from audit-infra. Fixed.** Three assertions checked only `error !== null`, so deleting `consume_rate_limit` outright kept the test green on PostgREST's `PGRST202`.                                                                                                                                                                                                                            | Done. All three assert `42501`.                                                                                                                                                                                                                                                          |
| F080 | Testing        | `tests/permissions.spec.ts:5`                                                                                 | High     | M   | **New, from audit-infra. Fixed.** The header called this the most important journey in the repo and named RLS inside the vector search; the test read `chunks` directly and never called `public.search`.                                                                                                                                                                                             | Done. It calls `search` as both readers. Verified by flipping `search` to security definer, which makes it fail.                                                                                                                                                                         |
| F081 | Security       | `supabase/schemas/80_functions.sql:8,20,35,52`                                                                | High     | S   | **New, from audit-infra. Fixed.** Four visibility predicates were `security definer` with `search_path = public` while every other definer used `''`, and the pgTAP assertion checked only that some `search_path=` entry existed.                                                                                                                                                                    | Done. All four are `''`, and the assertion compares the value.                                                                                                                                                                                                                           |
| F082 | Performance    | `supabase/schemas/50_entities.sql:39`, `51_dreams.sql:82`                                                     | Medium   | S   | **New, from audit-infra. Fixed.** 22 foreign keys lead on an unindexed column. `entity_mentions(chunk_id)` is the worst: every re-ingest deletes chunks, so the most frequent write in the product scanned that table per chunk.                                                                                                                                                                      | Done. Six indexes, on the keys a routine delete cascades through. The other sixteen are left alone and the migration says why.                                                                                                                                                           |
| F083 | Security       | `scripts/local-function-secrets.mjs:21`                                                                       | Critical | S   | **New, from audit-infra. Fixed.** `SB_TOKEN_ENC_KEY` fell back to `Buffer.alloc(32, 7)`, a constant in a public repo, as the AES-256-GCM key for provider tokens at rest. Any non-zero exit from Doppler triggered it, including a wrong token or a dropped network, announced by one `console.warn`.                                                                                                 | Done. A key is generated per machine and reused from the gitignored file, so yesterday's connections still decrypt. The comment claiming every default was public in `config.toml` was false and is gone.                                                                                |
| F084 | Security       | `supabase/schemas/95_grants.sql:52`                                                                           | High     | S   | **New, from audit-infra. Fixed.** `dream_links` had a table-wide UPDATE grant against a policy testing only `space_id`, so a member could rewrite `similarity`, `rationale`, `dream_run_id` and both document ids. The same shape as F001.                                                                                                                                                            | Done. `grant update (confirmed_at, dismissed_at)`, with two pgTAP assertions. The grant tripwire caught the change, as designed.                                                                                                                                                         |
| F085 | Correctness    | `supabase/schemas/70_usage_events.sql:16`                                                                     | Medium   | S   | **New, from audit-infra. Fixed.** `quantity` had no non-negative check, and every meter sums it, so a negative row is a smaller invoice and a plan limit that never arrives. `12_admin.test.sql` uses `-1000` as its own illustration.                                                                                                                                                                | Done. `check (quantity >= 0)`, named as Postgres names a column check so the declarative file and the migration agree.                                                                                                                                                                   |
| F086 | Performance    | `supabase/schemas/31_chunks.sql:37` and 3 others                                                              | Medium   | S   | **New, from audit-infra. Fixed.** Four indexes fully covered by the leading columns of another, on the two hottest write tables, right after six cascade indexes landed there.                                                                                                                                                                                                                        | Done. Dropped. `chunks_space_id_idx` stays: nothing else on that table leads with `space_id` and every policy filters by it.                                                                                                                                                             |
| F087 | Testing        | `scripts/integration-test.mjs:5`                                                                              | Medium   | M   | **New, from audit-infra. Fixed.** The docstring said the runner refuses to start when a browser run holds the stack. There was no such check, and the rule was documented in a comment and enforced nowhere.                                                                                                                                                                                          | Done. `tests/stack-lock.mjs`, taken by both runners, with a liveness check so a killed run does not block the next. Verified in both directions.                                                                                                                                         |
| F088 | Infra          | `scripts/gen-types.mjs:12`, `.prettierignore:8`, `supabase/config.toml:64`                                    | Low      | S   | **New, from audit-infra. Fixed.** A relative output path where every other script resolves from root; an ignore entry for a directory that does not exist; and a trailing `./schemas/*.sql` glob that was the only thing pulling in `95_grants.sql`, so grants applied last by alphabetical accident.                                                                                                 | Done. All three. Every schema file is named explicitly, so a new one that is not declared fails to apply rather than applying in the wrong order.                                                                                                                                        |
| F089 | Infra          | `.github/workflows/light-gate.yml:3`                                                                          | Low      | S   | **New, from audit-infra. Fixed.** `push: branches: ['**']` plus `pull_request:` ran the gate twice for every push to a PR branch, and the concurrency group cannot dedupe them because the two events carry different refs.                                                                                                                                                                           | Done. Push on `main`, pull_request everywhere else.                                                                                                                                                                                                                                      |
| F090 | Infra          | `.github/workflows/native.yml:21`                                                                             | Low      | S   | **New, from audit-infra. Fixed.** The job echoed that the native build is wired in `/ios` and `/android`, neither of which exists, and went green. It answered "is the native build working" with yes.                                                                                                                                                                                                | Done. It exits non-zero saying there is no native client yet and points at the spec.                                                                                                                                                                                                     |
| F091 | Infra          | `scripts/check-raw-color.mjs:129`                                                                             | Low      | S   | **New, from audit-infra. Fixed.** `exec` returns the first match per line per rule, so a class list carrying two raw colors was reported once and the printed count was short.                                                                                                                                                                                                                        | Done. `matchAll`. Verified with a line holding two violations: reported once before, twice now.                                                                                                                                                                                          |
| F092 | Correctness    | `scripts/seed-demo.mjs:47`                                                                                    | Low      | S   | **New, from audit-infra. Fixed.** Any `createUser` error was reinterpreted as "already there", so a bad service key or a rate limit walked the directory, found nothing, and reported "could not create or find" with the real cause buried. The listing also read only the first thousand accounts.                                                                                                  | Done. Only an already-registered error falls through, the directory pages, and a bad key now reports the JWT error itself.                                                                                                                                                               |
| F093 | Docs           | `docs/limits.md:113`                                                                                          | Low      | S   | **New, from audit-infra. Fixed.** It named two aggregate queries and called them admin panels. There is a third, `listVisibleSpaces`, and it is the Spaces page every signed-in person opens.                                                                                                                                                                                                         | Done. All three listed, with the blast radius corrected.                                                                                                                                                                                                                                 |
| F094 | Security       | `supabase/schemas/11_spaces.sql:44`                                                                           | High     | S   | **New, from audit-infra. Fixed.** The F001 grant stopped `authenticated` changing `kind`, `org_id` and `owner_user_id`, but a grant cannot restrain `service_role`, which every Edge Function holds. Promoting a team space to `kind = 'org'` makes `sync_org_space_membership` enrol every future member of the organization into it.                                                                | Done. A `before update` trigger raising on any change to the three, so the rule holds for every role. Nothing in the product writes them: the only updates to this table are the name and the dreaming toggle. Three pgTAP assertions, two of which failed before the trigger existed.   |

## Top five: if you fix nothing else

**F001, the cross-organization space move.** This is the only finding that
breaks the product's central promise. Reproduced:

```sql
set local role authenticated;
set local request.jwt.claims to '{"sub":"<member>","role":"authenticated"}';
update public.spaces set org_id = '<another org>' where id = '<my space>';
-- moved: 1
update public.spaces set kind = 'org' where id = '<my team space>';
-- kindnow: org
```

The fix is the same shape as the `space_id` work already in the tree:

```sql
revoke update on public.spaces from authenticated;
grant update (name, dreaming_enabled) on public.spaces to authenticated;

alter table public.spaces add constraint spaces_id_org_key unique (id, org_id);
alter table public.documents
  add constraint documents_space_in_org
  foreign key (space_id, org_id) references public.spaces (id, org_id) on delete cascade;
-- and the same for chunks, connections, ingest_jobs
```

**F002, the storage path.** One line closes it and a better fix removes the
class:

```ts
storagePath: z.string().regex(new RegExp(`^${spaceId}/`)),
```

**F003 and F004 together, the retry story.** Right now a transient failure is
terminal, a crashed worker is retired rather than retried, and the SQL comment
promising three attempts describes something no code path can reach. Delete the
JS sweep, write `queued` for a recoverable `SourceError`, and the cap becomes
real.

**F008, the eleven un-revoked functions.** Not current exposure. It is the next
regeneration, and it is the drift the lessons file already warns about.

**F009 and F010, the two dead meters.** A plan limit enforced by nothing and a
panel that says every document is dead. Both are one insert away.

## Quick wins

- [ ] F054 Delete six unused primitives and four unused dependencies
- [ ] F028 Parse the OTP `type` instead of asserting it
- [ ] F052 Check the `usage_events` insert result
- [ ] F036 Return a typed failure from the rate limiter
- [ ] F059 Fix the fire-and-forget comment that is awaited
- [ ] F056 Delete `remainingMs` and `isSpent`
- [ ] F057 Drop `ScopeOption.kind` and `scopeOptionSchema.url`
- [ ] F015 Delete the dead `refreshTokens`
- [ ] F029 Order and limit the embedded `ingest_jobs`
- [ ] F022 Wrap `getSessionContext` in `cache()`
- [ ] F066 Break the circular import
- [ ] F067 Record the webhook deviation

## Things that look bad and are actually fine

Kept from all three readers, because the reasoning is worth more than the list.

- **`requireWorkerCaller` returning early on unequal lengths.** The key length
  is not secret and comparing unequal buffers would read past the shorter one.
  Constant time for the property that matters.
- **The service role bypassing RLS in every handler.** Each one checks
  membership itself through `requireSpaceMembership`, which reads
  `space_members` directly, because `auth.uid()` is nobody under that key.
- **`dream_connections.ts:51` calling `search` outside the space-scoped
  writer.** Two independent checks rather than one: `space_filter` is the run's
  own space, and `documentsByIds` drops a foreign id before it reaches
  `insertLinks`.
- **`asRecord` returning `{}` rather than throwing.** A documented parse
  default so a provider's bad day is not a 500, not a swallowed error.
- **`maybePrune` sampling with `Math.random()`.** Housekeeping fired with
  `void Promise.allSettled`; determinism would make one unlucky caller pay.
- **`connections-callback` redirecting on every failure.** The caller is a
  browser mid-redirect and a JSON 500 in the address bar is worse.
- **Four drivers each holding their own reconnect message.** The display name is
  interpolated, the wording differs by design, and `leak_test.ts:220` enforces
  the property that matters across all four.
- **`report()` logging and continuing in `lib/openai/call.ts`.** A telemetry
  write must not turn a delivered answer into a failed request.
- **The `flatMap` drops in both view models.** They handle a torn read across two
  queries, and RLS already guarantees the pairing.
- **`lib/supabase/middleware.ts` listing `/api/stripe` as public.** Those routes
  skip the proxy and then do a stronger check themselves.
- **`page.tsx` and `components/ui/**` excluded from coverage.** Both arguments
  hold. The exclusion that is not argued anywhere is `hooks/**`, which is F025.
- **No side-stripe borders, no nested cards, no gradient text.** Looked for the
  banned patterns specifically. `grep border-l-|border-r-` across `*.tsx`
  returns nothing. The layout work is clean.

  The second half of that call was wrong: "the design findings are all confined
  to `components/ui/*`" held only for the patterns that were looked for. 61
  files outside it referenced compat aliases 172 times, which is F072. The
  reader checked for banned classes and not for classes that resolve through a
  file upstream intends to delete.

- **`pdf.ts` hand-rolling PDF parsing.** A megabyte of pdfjs and a cold start
  per ingest is the worse trade, and the caps bound a hostile file.

## Withdrawn after checking

Three findings from the infrastructure reader did not survive a read, and are
recorded here so nobody acts on them later.

- **`docs/retrieval.md` presents withdrawn measurements as standing.** It does
  not. The section is titled "Measured once, and partly under test" and states
  outright that only one of the three rows is still asserted, with the reasoning.
  The no-leak row is asserted at `20_search.test.sql:214-220`.
- **`docs/limits.md` disagrees with `vercel.json` on cron intervals.** It does
  not. The table at `limits.md:166-168` reads `*/2 * * * *`, `0 * * * *` and
  `0 2 * * *`, matching `vercel.json` exactly, and the reclaim comment in
  `80_functions.sql` reasons from the same two minutes.
- **`documents.last_retrieved_at` is still unwritten.** It is written, by
  `record_retrieval` (F010), which is `security definer` so a caller holding no
  UPDATE on `documents` can still cause the write. Verified: a document reads
  `null` and `0` before, a timestamp and `1` after.
- **The 1,005-chunk fixture and `enable_seqscan = off` in `20_search.test.sql`
  are dead weight left by a withdrawn assertion.** They are not. The count at
  line 106 runs on the small fixture, where the planner picks a sequential scan
  and filters perfectly, so it says nothing about the index path. `explain`
  under this exact setup reports `Index Scan using chunks_embedding_idx`. A
  comment in the file now says so, because this is the second time deleting them
  has looked reasonable.

## Open questions

1. **F033, the compat aliases.** The spec says rewrite the primitives onto
   semantic tokens. What makes the two hand-edited hooks safe is that a lint
   rule fires when `shadcn add` overwrites them; there is no equivalent tripwire
   for token classes. Add a check that fails when `compat.css` disappears while
   those classes are in use and record a deliberate deviation, or do the rewrite
   and accept it can be silently reverted?
2. **F021, retrieved content at system role.** Moving to user role is one line,
   but the `[n]` citation discipline currently rides on a system message
   adjacent to the passages, and there is no eval harness to say whether the
   safer shape costs answer quality.
3. **F002, narrow or correct.** The regex is one line. Deriving the path
   server-side is correct but requires agreeing a naming scheme with a vendored
   hook, and forces a decision between versioning and collision on re-upload.
4. **Vercel crons or pg_cron.** Answered: `pg_cron`. The three Vercel crons and
   the three routes behind them are gone, and the schedule is
   `supabase/schemas/96_schedules.sql`. Scheduling stays inside Supabase, which
   is the keynote's own argument and works for a stranger cloning the repo with
   no Vercel account.
5. **F017, the admin N+1.** A security-definer function returning id and email
   for an org is the clean fix but puts auth data behind a `public` function.
